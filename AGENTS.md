# Run Tests

You are explicitly required to run unit tests for this extension.

You can run them using the shortcut script `run-tests`.

# Rules Override

If AGENTS.dev.md exists, that file takes precendence over this one.
