; ComfyUI imports every subdirectory of comfy_node/ as a node package, so a
; .pytest_cache dropped here by a test run breaks comfy startup. Keep the
; cache out of the scanned tree.
[pytest]
cache_dir = /tmp/swarmui-videostages-pytest-cache
